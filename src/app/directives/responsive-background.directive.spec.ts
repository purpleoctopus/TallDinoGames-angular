import { ResponsiveBackgroundDirective } from './responsive-background.directive';

describe('ResponsiveBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
