import { ResponsiveImageDirective } from './responsive-image.directive';

describe('ResponsiveImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveImageDirective();
    expect(directive).toBeTruthy();
  });
});
